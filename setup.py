from setuptools import setup, find_packages

setup(
    name='pyassist',
    version='0.0.1',
    packages=find_packages(),

    #metadata
    author='Phill',
    description='A framework a python based voice assistant'
)